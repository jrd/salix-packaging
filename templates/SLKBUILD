# vim: syn=sh ft=sh et sw=2 sts=2 ts=2 tw=0:
#Packager: {pname} <{pemail}>

pkgname={name}
pkgver={ver}
pkgrel={rlz}{ptag}
{archvar}
source=({DLURL}) # FIXME
sourcetemplate={purl}/{pschema}/
docs=("readme" "install" "copying" "changelog" "authors" "news" "todo")
url={url}
#options=('noextract' 'nosrcpack' 'nostrip' 'noautodotnew')

#doinst() {
#
#}

slackdesc=\
(
#|-----handy-ruler------------------------------------------------------|
"$pkgname (FIXME)"
"FIXME"
)

build() {{{
{build}
}}}
