# vim: syn=python ft=python:
version_pattern = r"""grep '<a href'|grep -v 'http://'|md5sum|sed 's/  -//; s/^/MD5:/'"""
help_text = 'This just do a md5 sum of the links on the page. This is to be used when nothing more can be used.'
download_pattern = '{dl_url}/{pkgname}-{pkgver}.tar.gz'

